import { AIChat } from '@/components/ai/chat';

export default function AIPage() {
  return (
    <main className="h-[calc(100dvh-4rem)] w-full px-3 py-2 sm:px-4">
      <AIChat />
    </main>
  );
}
